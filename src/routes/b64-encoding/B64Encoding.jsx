import React from 'react';
import useDocumentTitle from '../../hooks/use-document-title';

export default function B64Encoding() {
  useDocumentTitle('Base64 encoding');
  return <div>B64Encoding</div>;
}
